# 1.表格标签

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset = "UTF-8">
    <title>111</title>
    <style> 
      .tables, .tables > tbody> tr> th, td{
        /* border: 1px solid red; 
        padding: 5px 15px; */
      }
      .table_box{
        
      }
      .centers{
        text-align: center;
      }
    </style>
  </head>
  <body > 
    align = 'left center right'  规定表格相对周围元素的对齐方式。
    border = 1或""  规定表格单元是否拥有边框，默认为""，表示没有边框
    cellpadding = 像素值  规定单元边沿与其内容之间的空白，默认1像素。
    cellspacing = 像素值  规定单元格之间的空白，默认2像素。
    width 像素值或百分比  规定表格的宽度。
    height 像素值或百分比  规定表格的高度。

      <div style= " top: 150px;">
        <table border="1" width='1000' height='250'  cellspacing = '0' cellpadding = "30"  align="center" class = "tables table_box">
          <thead>
            <tr>
              <th>1</th>
              <th>1</th>
              <th>1</th>
              <th>1</th>
            </tr> 
          </thead>
          <tbody> 
            <tr class="centers">
              <td>2</td>
              <td>2</td>
              <td>2</td>
              <td>2</td>
            </tr>
          </tbody>
        </table>
      </div>
  </body>
</html>

```

# 2.合并单元格

```html
合并单元格方式:
跨行合并: rowspan = "合并单元格的个数"
跨列合并: colspan = "合并单元格的个数”


<div>
  <table border="1" width = '600' cellspacing = '0' cellpadding = "50">
    <tbody>
      <tr>
        <td></td>
        <td colspan="2"></td> 
      </tr>
      <tr>
        <td rowspan="2"></td>
        <td></td>
        <td></td>
      </tr>
      <tr>
        <td></td>
        <td></td> 
      </tr>
    </tbody>
  </table>
</div>
```

# 3.表单标签

```html
type属性的属性值及其描述如下:

属性值     描述
button    定义可点击按钮(多数情况下，用于通过JavaScript启动脚本)。
checkbox  定义复选框。
file      定义输入字段和“浏览"按钮，供文件上传。
hidden    定义隐藏的输入字段。
image     定义图像形式的提交按钮。
password  定义密码字段。该字段中的字符被掩码。
radio     定义单选按钮。
reset     定义重置按钮。重置按钮会清除表单中的所有数据。
submit    定义提交按钮。提交按钮会把表单数据发送到服务器。
text      定义单行的输入字段，用户可在其中输入文本。默认宽度为20个字符。

<form action="" >
  
  <input type="text">
  // radio 单选按钮 可以实现多选一
  		checked => 选择	主要是针对单选和复选按钮的
  		maxlength = '6' 规定用户输入6个字符
  <input type="radio" name="s" checked = 'checked' maxlength = '6' >
  <input type="radio" name="s">
  <input type="radio" name="s"><br/>
  // checkbox 复选框 可以实现多选
  		checked => 选择 主要是针对单选和复选按钮的
  <input type="checkbox" checked = 'checked' >
  <select name="" id="">

  </select>
  <textarea name="" id="" cols="10" rows="1"></textarea>
</form>
```

# 4.下拉表单select

```html
<form>
  <select name="" id="">
    <option value="北京">北京</option>
    <option value="深圳" selected = 'selected'>深圳</option>
    <option value="上海">上海</option>
    <option value="广州">广州</option>
  </select>
</form>
```

# 5.文本域标签textarea

```html
<form>
  今日反馈：<textarea  name="" id="" cols="30" rows="3">
        cols 是一行可以写多少字
  			rows 是显示多少行
      </textarea>
</form>
```

# 6.设置字体font-family

```css
.box {
	font-family: "宋体"
}
```

# 7.设置字体大小font-size

```css
.box {
	font-size: 19px;
}
```

# 8.设置字体粗细font-weight

```css
normal:	正常的字体。相当于number为400。声明此值将取消之前任何设置
bold:	粗体。相当于number为700。也相当于b对象的作用
bolder: IE5+特粗体
lighter: IE5+细体
number: IE5+ 100 | 200 | 300 | 400 | 500 | 600 | 700 | 8o0 | 9oo

.box {
	font-weight:	bold;
}
```

# 9.设置字体样式font-style

```css
normal 默认值，浏览器会显示标准的字体样式 font-style: normal;
italic 浏览器会显示斜体的字体样式。
注意: 平时我们很少给文字加斜体，反而要给斜体标签( em, i )改为不倾斜字体
.box {
	font-style: normal | italic
}
```

# 10.font复合属性写法

```CSS
.pp {
  font-style: italic;
  font-weight: 800;
  font-size: 40px;
  font-family: Cambria, Cochin;
}
.ppp {// 等同上行
  /* font: font-style font-weight font-size/line-height font-family; 必须按顺序写*/
  font: italic  800  40px  Cambria, Cochin; 
}
```

# 11.color

```css
.box {
	color: red;		/*预定颜色值*/
	color: #FF000;  /*十六进制*/
	color: rgb(48,174,199/.5);  /*RGB代码*/
}
```

# 12.文字对齐text-align

```css
.box{
  text-align: center|left|right;
}
```

# 13.装饰文本text-decoration

```css
text-decoration 属性规定添加到文本的修饰。可以给文本添加下划线、删除线、上划线等。
none    				默认。没有装饰线(最常用)
underline				下划线。链接a自带下划线（常用)
overline				上划线。(几乎不用)
line-through		删除线。(不常用)

.box {
  text-decoration: none
}
```

# 14.文本缩进text-indent

```css
.box {
  text-indent: 2em;
}
```

# 15.行间距line-height

```css
.box {
  line-height: 26px;
}

font: 16px/2	
font: 16px/16px
```

# 16选择器

```css
/*后代选择器*/
ol li {
  color: red;
}
ol li a {
  color: red;
}

/* 子元素选择器*/
div > p { 
  color: red;
}

/* 并集选择器*/
div, p {
  color: red;
}

/* 伪类选择器*/
div span>a:hover { /* 当鼠标经过div时 改变div下的span下的a的color为red*/
  color: red;
}
```

# 17.background

```css
1.参数是方位名词
	如果指定的两个值都是方位名词，则两个值前后顺序无关，比如left top和top left效果一致
	如果只指定了一个方位名词，另一个值省略，则第二个值默认居中对齐

2.参数是精确单位
	如果参数值是精确坐标，那么第一个肯定是x坐标，第二个一定是y坐标
	如果只指定一个数值，那该数值一定是x坐标，另一个默认垂直居中

3.参数是混合单位
	如果指定的两个值是精确单位和方位名词混合使用，则第一个值是x坐标，第二个值是y坐标

background-color: transparent; (透明) // 默认值

background-image: url();	
background-repeat: no-repeat; // 图片不平铺
background-repeat: repeat-x;  // 横向平铺
background-repeat: repeat-y;	// 纵向平铺

// 如果指定的两个值都是方位名词，则两个值前后顺序无关，比如left top和top left效果一致
background-position: top | center | bottom = x  left | center| right = y // 方位名词
// 如果只指定一个数值，那该数值一定是x坐标，另一个默认垂直居中
background-position: 20px 50px // 精准单位
```

# 18.background-attachment

```css
scroll 背景图像是随对象内容滚动心
fixed  背景图像固定

background-attachment: scroll | fixed
```

# 19.优先级

```css
选择器								选择器权重
继承 或者 *    			  0000
元素选择器     			 0001
类选择器，伪类选择器   0010
ID选择器       			0100
行内样式style=""		 1000
!important    			∞无穷大		使用 color: red !important;
```

# 20.border

```css
border-collapse: collapse;  // 合并相邻边框
```

# 21.border-radius圆角

```css
border-radius: 左上角 右上角 右下角 左下角
border-radius: 10px; /* top-left:10px top-right:10px bottom-right:10px bottom-left:10px*/
border-radius: 10px 20px; /* 对角线：左上角and右下角 右上角and左下角*/
border-radius: 10px 30px 60px; /* 左上角  右上角and左下角  右下角*/
border-radius: 10px 10px 10px 10px;
```

# 22.box-shadow阴影

```css
h-shadow 		必需。水平阴影的位置。允许负值。
v-shadow 		必需。垂直阴影的位置。允许负值。
blur 				可选。模糊距离(影子虚实度)。
spread 			可选。阴影的尺寸(影子大小)。
color 			可选。阴影的颜色。请参阅CSS颜色值。
inset 			可选。将外部阴影(outset)改为内部阴影。

box-shadow: 10px 1px 30px 10px red inset;

box-shadow: 10px 1px 20px 4px rgba(0, 0, 0, .5);
```

# 23.text-shadow文字阴影

```css
h-shadow 		必需。水平阴影的位置。允许负值。
v-shadow 		必需。垂直阴影的位置。允许负值。
blur 				可选。模糊的距离。
color 			可选。阴影的颜色。参阅CSS颜色值。

text-shadow: 1px 0px 5px rgba(255, 255, 255, .5)
```

# 24.float浮动

```css
float: none(默认值) | left | right;
任何元素添加浮动都具有行内块特性
浮动的盒子只会影响浮动盒子后面的标准流，不会影响前面的标准流


/*清除浮动（在浮动元素的父级添加伪元素）*/
.divcss5::before, .divcss5::after{	// 伪元素清除浮动
  content: "";
  clear: both;
  display: inline-block;
  visibility: hidden;	// 隐藏伪元素内容content
}
.divcss5 {
  *zoom: 1; // 兼容IE6 7 （清除浮动）
}
```

# # 

```css

```

# # 

```css

```

# # 

```css

```

# # 

```css

```

# # 

```css

```

# # 

```css

```

# # 

```css

```

# # 

```css

```

# # 

```css

```

# # 

```css

```

# # 

```css

```

